#include<stdio.h>
#include<math.h>
int main()
{
 int num,power;
 printf("Enter the number: ");
 scanf("%d",&num);
 printf("Entered the power ")
 scanf("%d",&power)
 printf("The given power of %d is :%d",num,pow(num,power));
 return 0;
}
